# QA.md

Manual QA checklist and running gate-review log, per the delivery plan's operating model. QA re-runs each milestone's acceptance criteria independently — not the same session that built the feature.

---

## Milestone gate log

| Milestone | Acceptance criteria (from `claude.md` §7) | Result | Notes |
|---|---|---|---|
| M0 Scaffold | `docker compose up` → `curl /healthz` = 200; CI green | **Pass** | Verified 2026-08-19: `docker compose up -d --build` → all 3 containers healthy → Alembic migration 0001 applied cleanly inside the api container → `curl localhost:8000/healthz` → `200 {"status":"ok"}`. CI green on `syedautomates/synctalk` main (ruff, mypy, migration-against-real-postgres, pytest all pass). One lint-fix cycle needed (E501 line-length) before CI went green — see `DECISIONS.md`. |
| M1 Profiles/uploads/validation | Upload golden fixtures → profile checklist all green; upload a deliberately blurry photo → clear rejection message | **Pass** | Verified 2026-08-19 via real `docker compose` stack (not mocked). Bad-photo half: synthetic 200×200 blank/faceless photo → all 3 expected rejections fired with clear messages (low resolution, blur, no face). Golden-fixture half, all real files: 3/5 real candidate photos passed (other 2 correctly rejected — visually softer, confirmed by face-region sharpness scoring); real 51.5s/4K/24fps reference video passed (`single_face_ratio=1.0`, `hands_ratio=0.885`, 12 frames extracted, primary reference frame visually confirmed sharp/upright/natural); real 54.6s voice sample passed (mean volume -16.3dB, well clear of the near-silence cutoff); consent confirmed via the live API. **Final checklist: `photos.ok=true`, `reference_video.ok=true`, `voice_sample.ok=true`, `consent=true`, `ready=true`.** Two real bugs found and fixed via this real-data testing (neither caught by the earlier synthetic test) — see `DECISIONS.md`: (1) sharpness scored on the whole frame instead of the face region, falsely rejecting genuinely-sharp zoomed-in photos with plain backgrounds; (2) phone-video rotation metadata wasn't applied to decoded frames, so face detection ran sideways and `single_face_ratio` measured 7.7% instead of the true ~100%. |
| M2 Voice pipeline | Tagged text → audible emotional delivery; 12k-char script chunks into ≥2 calls and concats into one seamless file | **Pass** | Verified 2026-08-19 against the real ElevenLabs API via the actual `/profiles/{id}/voice` endpoint (not a raw script): real IVC created from the real passed voice sample (`elevenlabs_voice_id=UBxdikrk51iTH7SzcoVC`); consent gate confirmed (`403` on a no-consent profile, before any ElevenLabs call fired); 12k-char chunking verified offline (3 chunks, all under the 4,500-char safety margin, exact text reconstruction); real TTS with `"[excited] Hello! [laughs] This is a test."` synthesized against the cloned voice, saved to `assets/test/emotion_test_output.mp3`; real multi-chunk concat path exercised (two real TTS calls → ffmpeg concat), combined duration 3.40s matched the sum of parts (1.65s + 1.80s). Director listened to `emotion_test_output.mp3` (2s) and confirmed the emotional delivery sounds right. M2 fully closed. |
| M3 Look generation | Prompt *"tech home office, condenser mic on a boom arm entering from the left, black polo shirt"* on the golden profile → ≥1 of 4 candidates keeps identity recognizably intact (manual check) with mic on the correct side | **Pass** | Verified 2026-08-19 end-to-end on real infrastructure — a provisioned RunPod A100 80GB PCIe pod (`$1.39/hr`, Secure Cloud), real Qwen-Image-Edit-2511 weights, real DB-backed job queue, real look-generation request against the golden profile's real `primary_ref_image_key`. Job flow: `POST /profiles/{id}/looks` → job enqueued → worker long-polled, leased, downloaded reference images via presigned URLs, generated 4 candidates (seeds 0–3, 40 steps each) → uploaded outputs via presigned PUT → `POST .../complete` → `Look.candidate_keys`/`status` updated correctly. All 4 candidates (not just ≥1) visually confirmed: mic correctly on the left in every candidate, black polo shirt, tech home-office setting with monitors/code visible, and strong identity match against the real reference photo (same curly dark hair, beard shape, facial structure, build) in all 4 — comfortably clears the "≥1 of 4" bar. `POST /looks/{id}/approve` exercised for real, confirming the full user-facing flow closes. Total RunPod spend for the whole M3 session: **~$2.15** (balance $10.21 → $8.06), pod terminated immediately after verification. Two real bugs found and fixed via this live run (see `DECISIONS.md`): a torch/CUDA driver-version mismatch, and worker HTTP calls with no timeout hanging forever after a tunnel blip. |
| M4 Claude orchestrator | Unit tests with mocked Claude responses for schema validation; live test with brief *"sad, subdued, this is a farewell message"*-style emotional arc + a multi-paragraph script → tags like `[sad]`/`[sighs]` appear at plausible points, script wording unchanged (diff ignoring bracketed tags), `style_prompt` contains "talking" and no appearance descriptors | **Pass** | Verified 2026-08-19. Unit half: 9 mocked tests covering both pydantic validators (chunk-concatenation-equals-tagged_script, chunk ≤2500 chars, style_prompt contains "talking") and `orchestrate()`'s retry-then-fail behavior against a mocked Anthropic client — no live API calls in the automated suite. Live half: real call against the actual golden-fixture `emotion_brief.txt` (serious→warm→confident→calm arc) + `script.txt` (270-word GoHighLevel/Zapier script) via the real `claude-sonnet-5` API. Wording-diff check (regex-normalized word sequence, tags stripped): **234/234 words identical** between original script and tagged output — zero wording drift. 4 tags inserted (`[warmly]`, `[smiling]`, `[confident]`, `[sincere]`), each landing at a point matching the brief's described arc, sparse relative to the ~234-word script (not over-tagged). `style_prompt` = *"Talking, speaking directly to camera; begins composed and matter-of-fact... "* — contains the required phrase, zero appearance/clothing words. `tts_chunks` reconstructed `tagged_script` exactly (1 chunk, 1362 chars, under the 2500-char limit). One real bug found and fixed (see `DECISIONS.md`): `claude.md`'s "temperature ≤0.4" instruction is stale — `claude-sonnet-5` returns `400` on any `temperature` value; removed the parameter entirely. |
| M5 GPU worker: video generation + upscale | Golden profile + approved look + audio → finished 4K mp4 with correct lip sync (manual check), no frame jumps, audio in sync at start AND end | **Pass** (budget-scoped — see `DECISIONS.md`) | Verified 2026-08-19 end-to-end on a provisioned RunPod A100 80GB PCIe pod (dev/correctness rig, not the 5×H100 production node — throughput/latency not benchmarked, only correctness). Real pipeline: `video_generation` (LiveAvatar, `Wan2.2-S2V-14B` + LoRA) against the golden profile's real M3-approved look image + real ElevenLabs TTS audio (6.53s, real cloned voice, `[confident]` tag) → multi-clip generation auto-covering the full audio → `upscale` (SeedVR2, 3B fp16) → 2160p output with original audio muxed back in. Visually confirmed at three points (start, middle, and — critically — the very last frame): natural eye blinking, mouth shapes tracking the audio, natural hand gesture matching the style_prompt, identity/scene held consistent with the approved M3 look throughout, no frozen/missing frames near the end. Output: 2776×2160 h264/AAC, video 6.56s vs. audio 6.48s (0.08s drift, independently reconfirmed via `ffprobe` on the downloaded file — well under the 0.2s limit). Six real bugs found and fixed via this live run (see `DECISIONS.md`) — most notably a duration-validation bug that would have silently shipped a video that freezes before the audio ends. **Descope from the full plan**: only LiveAvatar was live-tested (LongCat's avatar path needs 2 GPUs per its own docs — deferred); no throughput/GPU-seconds benchmarking; a formal quantitative LiveAvatar-vs-LongCat bake-off is deferred until production-scale GPU budget exists. Total RunPod spend for the whole M5 session: **~$2.45** (balance $8.01 → $5.56), pod terminated immediately after verification. |
| M6 End-to-end wiring + wizard | A person who has never seen the code completes photo→video entirely through the UI | **Pass, GPU stage descoped from this run** | Verified 2026-08-19 via a live headless-browser (Playwright) walkthrough against the real running Next.js dev server + real FastAPI backend (no mocks): login → use existing golden profile → `/train` correctly renders the checklist as `ready=true` (all 3 items passed ✓) → "Continue to Looks" → `/looks` → submitted a real scene prompt, which hit the real `POST /profiles/{id}/looks` endpoint and created a real job → UI correctly rendered the queued/generating state without crashing → navigated directly to `/create` → correctly blocked with "No approved look yet — go back to the Looks step and approve one first." (this exact check was broken and got fixed as part of this run — see below). Zero browser console/page errors on the final run. **Descope**: no GPU worker pod was running during this session (cost discipline — see `[[synctalk_confirm_before_spend]]` memory), so the look-candidate-generation → approve → create-video → finished-4K-video path was not driven end-to-end through the UI with a real render; that GPU-backed leg was already verified directly against the API in M5's row above, and the orchestrate→TTS→enqueue wiring in `POST /videos` was verified via direct API calls (see `DECISIONS.md`'s M6 section). A full live click-through of every step including a real rendered video is still worth doing once a GPU pod is spun up again for M7/final acceptance. Two real bugs found and fixed via this browser run (see `DECISIONS.md`): a self-inflicted hydration mismatch on every wizard page (introduced by an earlier lint fix), and the `/create` approval guard checking only for a stored look ID rather than actual approval. Backend `ruff`/`mypy`/`pytest` and frontend `eslint`/`tsc` all clean after the fixes. |
| M7 Hardening | Kill the worker mid-render → job is reclaimed and retried once → completes; duplicate `POST /videos` with same idempotency key → one job | **Pass** | Verified 2026-08-19, all against the real running stack (not just mocks) unless noted. **Idempotency**: duplicate `POST /profiles/{id}/looks` and duplicate `POST /videos` with the same `Idempotency-Key` each returned byte-identical responses; direct `psql` queries confirmed exactly one `Look`/`Job` row and exactly one `VideoRequest`/`Job` row respectively — and the video request's `cost_ledger` shows real Claude+ElevenLabs usage that only happened once (215 output tokens, 43 chars, 3.24s audio), proving the duplicate was served from cache rather than re-billing. **Moderation**: a look prompt naming a real public figure and a video script impersonating a named real person were both correctly rejected with 400 before any GPU job, Claude call, or ElevenLabs call fired (zero cost on the rejected path); 4 mocked unit tests cover the flagged/clean/API-error/unparseable-response branches. **Structured logging**: `GET /metrics` sent with `X-Request-ID: test-req-123` came back with that header echoed, and the container's JSON log output contained the matching `request_id`-tagged "request completed" line with method/path/status/duration — confirmed the contextvar propagation actually works. **S3 lifecycle**: `mc ilm rule list` confirmed a real tag-filtered 7-day expiry rule registered in MinIO; uploading a deliberately bad photo through the real API and checking `mc tag list` confirmed the resulting failed-validation object was actually tagged for that rule to catch (real deviation from claude.md's literal "temp copies" wording — see `DECISIONS.md`, raw uploads in this design aren't temporary, only failed-validation ones are safe to expire). **Lease-reclaim**: `tests/test_jobs.py` (real Postgres, not mocked) simulates a crashed worker (backdated `lease_expires_at`) and confirms the same job is reclaimed by a second lease and completes, plus a second test confirming a job that exhausts `MAX_ATTEMPTS` (2) is marked permanently failed rather than retried forever. 19 automated tests total (up from 9 at M6), `ruff`/`mypy` clean, migration `0003` applies cleanly. |

---

## Golden fixtures (M8, pulled forward — tracked here as they're produced)

| Fixture | Status | Notes |
|---|---|---|
| 2–3 zoomed-in photos | ✅ Uploaded & validated | 5 candidates transferred to `Downloads/`; 3 (`IMG_9582/9583/9585`) passed real validation and are copied into `assets/test/photo_{1,2,3}.jpeg` (local only, gitignored — see `DECISIONS.md`). |
| 30–60s reference video (face/lips/hands) | ✅ Uploaded & validated | Landed directly in the repo folder as `IMG_9587.mov`, moved to `assets/test/reference_video.mov` (local only, gitignored). 51.5s, 4K, 24fps. Passed real validation after the rotation-metadata fix — see `DECISIONS.md`. |
| ≥30s voice sample | ✅ Uploaded & validated | `assets/test/voice_sample.mp4` (local only, gitignored). 54.6s, mean volume -16.3dB. Passed real validation. |
| ~300-word test script | ✅ Locked | `assets/test/script.txt` (tracked in git — small text file). Repurposed from an existing Upwork/HeyGen profile-video script — GoHighLevel/Zapier automation topic, ~270 words, natural emotional arc (serious → warm → confident → calm). |
| Emotion/movement brief example | ✅ Locked | `assets/test/emotion_brief.txt` (tracked in git). Derived from the same source script's segment-direction table — serious/documentary opening, warm reveal, confident/rhythmic build, calm-certain close, minimal natural gestures throughout. |
| Consent confirmation | ✅ Confirmed | Director confirmed both the consent copy wording and personal consent. Copy logged in `DECISIONS.md`. |

## Manual QA checklist (per M8 — to execute once M5/M6 are complete)

- [ ] Identity match at 0s / 30s / 60s of a generated video
- [ ] Lip-sync spot check at 3 timestamps
- [ ] Hands move naturally in generated output
- [ ] No teeth artifacts
- [ ] 4K file plays in QuickTime + Chrome
- [ ] Audio drift ≤0.2s at end of video
